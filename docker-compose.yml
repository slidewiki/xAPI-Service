template:
  image: slidewiki/xapi-service
  restart: on-failure:5
  expose:
    - "80"
  environment:
    - APPLICATION_PORT=80
    - DATABASE_PORT=27017
    - VIRTUAL_HOST=xapiservice.experimental.slidewiki.org
